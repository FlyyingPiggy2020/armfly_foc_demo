FOC开环能够运行，但是闭环后震荡。

检查ADC的接线是否正确。


1.锁轴，给虚拟的角度，例如固定当前角度为0。
2.关掉PI直接给V_d=0.12;V_q=0。
3.如果接线正常，理论上应该读到a相电流是一个正数，ib是一个负数幅值为ia的一半。

但是我实际上读到a相的是负数。


1.占空比 发波设置的不正确
2.电流采样反了。



d=0.12 q=0, theta = 0;
alpha = 0.12, beta=0
a = 0.12 ; b = -0.06; c = -0.06